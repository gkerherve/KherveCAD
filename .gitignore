__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
build/
dist/
*.kcad.bak
# written by packaging/build_installer.py so the frozen build knows its
# version; the checkout derives it from git instead.
khervecad/VERSION
# cached OpenSCAD portable ZIP (~21 MB), re-downloaded on demand
packaging/vendor/

# reference drawings the car builder is measured from: copyrighted,
# private reference only - the measurements live in khervecad/car_sheets.py
Blue Prints/
