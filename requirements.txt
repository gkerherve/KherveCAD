PyQt5>=5.15
qtawesome>=1.3
numpy>=1.26         # runtime: Cloth, Remesh, Shrinkwrap, push/pull, and the manifold3d bridge
manifold3d>=3.0     # exact booleans, Bevel, Wireframe, push/pull (csg.py); approximated if absent
pygit2>=1.14        # optional: per-document Git (commit/push); degrades if absent
certifi>=2024.2     # CA bundle for a frozen build (updater, map import); optional

# development only: khervecad.tools.carblueprint traces a car's shape off a
# blueprint (carsheet also wants scipy + scikit-image, not listed: the app
# never imports either tool).
pillow>=10
