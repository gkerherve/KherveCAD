PyQt5>=5.15
qtawesome>=1.3
pygit2>=1.14        # optional: per-document Git (commit/push); degrades if absent

# development only: khervecad.tools.carblueprint traces a car's shape
# off a blueprint. The app itself never imports either.
numpy>=1.26
pillow>=10
